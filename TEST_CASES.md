# 🧪 IRM — Comprehensive Test Cases & QA Specification
> **Incoming Raw Material Tracking System (ระบบติดตามการรับวัตถุดิบ)**  
> **องค์กร:** Window Asia Public Company Limited (Window Asia PCL.)  
> **เวอร์ชัน:** 1.0 Production-Ready (สิงหาคม 2026)

---

## 📑 สารบัญชุดการทดสอบ (Test Suites Overview)

| รหัสชุดทดสอบ | โมดูล / ฟังก์ชันการทำงาน | จำนวน Test Cases |
| :--- | :--- | :---: |
| **TC-AUTH** | ระบบยืนยันตัวตนและการจัดการสิทธิ์ (Authentication & RBAC) | 5 |
| **TC-OP** | หน้าจอติดตามรับวัตถุดิบและจัดรอบส่ง (Operation Management) | 12 |
| **TC-ITEM** | ทะเบียนรหัสสินค้าและ Lead Time (Item Master) | 6 |
| **TC-SUP** | ทะเบียนผู้จำหน่ายและสิทธิ์ส่งเกิน (Supplier Master & Over-Delivery) | 8 |
| **TC-PORTAL** | ระบบลิงก์และหน้าจอตอบกลับของคู่ค้า (Supplier Portal & Security) | 8 |
| **TC-HIST** | ประวัติรายการปิดยอดและ Audit Trail (History & Audit Logs) | 5 |
| **TC-SCHED** | ระบบงานอัตโนมัติตามเวลา (APScheduler & Background Cron) | 4 |
| **TC-TG** | การแจ้งเตือน Telegram กลาง (Centralized Telegram & Emojis) | 7 |
| **TC-ADMIN** | การตั้งค่าระบบและการจัดการผู้ใช้ (System Settings & Users) | 6 |
| **TC-SEC** | ความปลอดภัยและการจัดการข้อมูลผิดพลาด (Security & Edge Cases) | 5 |
| **รวมทั้งหมด** | **10 หมวดหมู่การทดสอบครอบคลุมทั้งระบบ** | **66 Test Cases** |

---

## 🔐 1. ชุดทดสอบระบบยืนยันตัวตนและสิทธิ์ (TC-AUTH)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-AUTH-01** | เข้าสู่ระบบด้วยรหัสถูกต้อง (Valid Login) | 1. เข้าหน้า `/login`<br>2. กรอก `admin` / `irm@2026`<br>3. กดปุ่ม เข้าสู่ระบบ | เข้าสู่ระบบสำเร็จ บันทึก JWT Token ใน Cookie และ Redirect ไปหน้า `/operation` | Pass |
| **TC-AUTH-02** | เข้าสู่ระบบด้วยรหัสผิด (Invalid Login) | 1. กรอก `admin` / `wrongpassword`<br>2. กดปุ่ม เข้าสู่ระบบ | แสดง Alert สีแดงแจ้งเตือนว่าชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง และไม่ให้เข้าสู่ระบบ | Pass |
| **TC-AUTH-03** | การจำกัดสิทธิ์ตามกลุ่ม (Group RBAC) | 1. Login ด้วย User กลุ่ม `Viewer`<br>2. เปิดหน้า `/operation` | มองเห็นข้อมูลได้ แต่ปุ่มแก้ไข แตกงวดส่ง หรือ Unlock จะถูก Disable หรือซ่อนไว้ | Pass |
| **TC-AUTH-04** | การป้องกันหน้า Admin (Admin Route Guard) | 1. Login ด้วย User ฝ่ายจัดซื้อทั่วไป<br>2. พยายามเข้า URL `/admin/settings` หรือ `/admin/users` | ระบบบล็อกการเข้าถึงและ Redirect กลับหน้าหลัก หรือแสดง HTTP 403 Forbidden | Pass |
| **TC-AUTH-05** | ออกจากระบบ (Logout) | 1. กดปุ่ม Logout ที่แถบด้านซ้าย | ล้าง Session Cookie ทั้งหมด และ Redirect กลับสู่หน้า `/login` ทันที | Pass |

---

## 📋 2. ชุดทดสอบหน้า Operation (TC-OP)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-OP-01** | แถบเลื่อน Mirror Scrollbar บนจอ 14" | 1. ย่อหน้าจอขนาด 14" (1366x768)<br>2. เลื่อน Scrollbar ด้านบนสุดของตาราง | ตารางด้านล่างเลื่อนซ้าย-ขวาตามแบบ Synchronized Real-time โดยไม่ต้องเลื่อนลงไปล่างสุด | Pass |
| **TC-OP-02** | การตรึงคอลัมน์ (Sticky Columns) | 1. เลื่อนตารางไปทางขวาสุด | คอลัมน์ `#`, `PO No. / Date`, และ `Group` ยังคงตรึงอยู่ทางซ้าย ไม่เลื่อนหลุดจอ | Pass |
| **TC-OP-03** | การแสดงสีสถานะแถว (Visual Row Highlighting) | 1. ตรวจสอบแถวในตาราง | 🟢 **เขียว:** รายการที่ระบุวัน/จำนวนส่งแล้ว<br>🟠 **ส้ม:** รายการที่ Supplier เพิ่งตอบกลับ<br>⚪ **เทา:** รายการที่ยังไม่ได้ระบุวัน | Pass |
| **TC-OP-04** | Quick Filter Tabs | 1. คลิกแท็บ `มาใหม่วันนี้`<br>2. คลิกแท็บ `ปรับเปลี่ยนแล้ว`<br>3. คลิกแท็บ `Sup ตอบกลับ` | ตารางกรองแสดงเฉพาะรายการตามแท็บที่เลือกทันที พร้อมตัวเลขจำนวนนับที่ถูกต้อง | Pass |
| **TC-OP-05** | แตกงวดส่งปกติ (Inline Split Sub-Items) | 1. กดปุ่ม `+` ที่แถว PO<br>2. ระบุงวด 1: วันที่ A จำนวน 500<br>3. กดเพิ่มงวด 2: วันที่ B จำนวน 500<br>4. กดปุ่ม บันทึก | บันทึกรอบส่ง 2 งวดลงฐานข้อมูล ยอดรวมแสดง 1,000 และเปลี่ยนสถานะเป็นแถบสีเขียว | Pass |
| **TC-OP-06** | ตรวจสอบผลรวมยอดส่งปกติ (Over-Qty Limit Check) | 1. Supplier ที่ไม่มีสิทธิ์ Over-Delivery<br>2. พยายามกรอกผลรวมงวดส่งเกินยอดสั่งซื้อ | แสดง Alert เตือนสีแดงว่ายอดรวมเกินยอดคงเหลือ และไม่ให้บันทึก | Pass |
| **TC-OP-07** | แตกงวดส่งเกินยอดสั่งซื้อ (Over-Delivery Allowed) | 1. Supplier ที่เปิดสิทธิ์ `Allow Over-Delivery`<br>2. กรอกยอดรวม 10,342 (จากยอด PO 10,340)<br>3. กดปุ่ม บันทึก | ระบบอนุญาตให้บันทึกสำเร็จ แสดงแท็กสีม่วง `ส่งเกิน (+2)` ในหน้าจอ | Pass |
| **TC-OP-08** | การกดยืนยันรับทราบข้อมูล (Accept Supplier Response) | 1. แถวที่มีสถานะ `Sup ตอบกลับ` กดปุ่ม `✓ Accept` | 1. ปุ่ม Accept หายไปทันที<br>2. รายการเปลี่ยนสถานะเป็น Confirmed<br>3. **ผู้ปรับปรุงล่าสุดยังคงเป็น `🏢 Supplier` พร้อมเวลาจริง (ไม่เปลี่ยนเป็น Admin)** | Pass |
| **TC-OP-09** | การปลดล็อคโดยจัดซื้อ (Manual Unlock Override) | 1. แถวที่มีแม่กุญแจ `🔒 รอ Sup ตอบ`<br>2. กดปุ่ม `[ 🔓 ]`<br>3. ระบุเหตุผลใน Modal แล้วกดยืนยัน | เรคอร์ดถูกปลดล็อคให้จัดซื้อแก้ไขได้ทันที และบันทึกเหตุผลลงใน Audit Log | Pass |
| **TC-OP-10** | คัดลอกลิงก์เฉพาะ PO (Copy Single-PO Link) | 1. กดไอคอนลูกโซ่ `🔗` ที่แถวรายการ | ลิงก์ถูกคัดลอกลง Clipboard และแสดงไอคอนเครื่องหมายถูกสีเขียว 2 วินาที | Pass |
| **TC-OP-11** | ค้นหาข้อมูล Real-time (Global Search) | 1. พิมพ์เลข PO, รหัสสินค้า, หรือชื่อ Supplier ในช่องค้นหา | ตารางกรองผลลัพธ์ทันทีแบบไม่ต้องกดปุ่ม Search | Pass |
| **TC-OP-12** | รายการใหม่ประจำวัน (New Badge Clearance) | 1. รายการที่มีป้าย `[✨ NEW]`<br>2. จัดซื้อทำการระบุวันส่งหรือกดบันทึก | ป้าย `[✨ NEW]` จะหายไปโดยอัตโนมัติ | Pass |

---

## 📦 3. ชุดทดสอบทะเบียนสินค้า (TC-ITEM)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-ITEM-01** | แสดงผลครบทั้ง 7 กลุ่มสินค้า | 1. เปิดหน้า `/items`<br>2. ตรวจสอบรายชื่อกลุ่มและสี | แสดงครบ 7 กลุ่ม: `RM-ALU/UPVC (เต็ม)`, `HW`, `FG-Non BOI`, `RM-กระจก`, `SP - Sparepart`, `RM-เหล็กดัด`, `HW-Partner` | Pass |
| **TC-ITEM-02** | Quick Group Filter Tabs | 1. คลิกแท็บ `HW`<br>2. คลิกแท็บ `RM-กระจก` | แสดงเฉพาะสินค้าในกลุ่มที่เลือก พร้อมสี Badge ที่ตรงตามมาตรฐาน | Pass |
| **TC-ITEM-03** | แก้ไข Lead Time และวันแจ้งเตือน | 1. กดไอคอนแก้ไขที่แถวสินค้า<br>2. ปรับ Lead Time = 45 วัน, Notify = 5 วัน<br>3. กดปุ่ม บันทึก | บันทึกค่าใหม่ลงฐานข้อมูล และตารางแสดงค่าใหม่ทันที | Pass |
| **TC-ITEM-04** | ปลดสถานะสินค้าใหม่ (Accept New Item) | 1. สินค้าที่มีป้าย `[✨ NEW]` กดปุ่ม `Accept` | ป้าย `[✨ NEW]` หายไป และสถานะสินค้าเข้าสู่ Master ปกติ | Pass |
| **TC-ITEM-05** | ส่งออกไฟล์ CSV สินค้า (Export CSV) | 1. กดปุ่ม `Export CSV` | ดาวน์โหลดไฟล์ชื่อ **`IRM_Item_Master_Export_YYYYMMDD_HHMMSS.csv`** พร้อมภาษาไทย UTF-8 สมบูรณ์ | Pass |
| **TC-ITEM-06** | นำเข้าไฟล์ CSV สินค้า (Import CSV) | 1. เตรียมไฟล์ CSV ที่แก้ไข Lead Time<br>2. กดปุ่ม `Import CSV` และเลือกไฟล์ | ระบบอัปเดตข้อมูลทุกรายการในไฟล์ พร้อม Alert สรุปจำนวนรายการที่สำเร็จ | Pass |

---

## 🏭 4. ชุดทดสอบทะเบียนผู้จำหน่าย (TC-SUP)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-SUP-01** | แก้ไข Email และเบอร์โทรศัพท์ | 1. กดไอคอนแก้ไขที่ Supplier<br>2. กรอก Email และกดบันทึก | บันทึก Email สำเร็จ และมีป้ายเขียว `มี Email แล้ว` | Pass |
| **TC-SUP-02** | ล้างตัวอักษรภาษาไทยใน Email อัตโนมัติ | 1. กรอก Email ตกภาษาไทย เช่น `ืn.chaiwat@gmail.com`<br>2. กดบันทึก | ระบบแปลงเป็น `n.chaiwat@gmail.com` ให้โดยอัตโนมัติก่อนบันทึก | Pass |
| **TC-SUP-03** | ความปลอดภัยของ Email จาก SAP Sync | 1. ทำการ Run Sync ข้อมูลจาก SAP | Email และ Group ที่ User แก้ไขใน IRM **ไม่ถูก SAP เขียนทับ 100%** | Pass |
| **TC-SUP-04** | เปิด-ปิดสิทธิ์ส่งเกิน (Toggle Allow Over-Delivery) | 1. คลิกสวิตช์ `อนุญาตให้ส่งเกิน`<br>2. กดบันทึก | สถานะสิทธิ์เปลี่ยนเป็นเปิด/ปิดทันที และมีผลต่อ Portal ของ Supplier รายนั้น | Pass |
| **TC-SUP-05** | ส่งอีเมลรายบริษัท (Send Single Email) | 1. กดปุ่ม `📧 ส่ง Email` ที่ Supplier | ระบบส่งอีเมลจริงด้วย UTF-8 ผ่าน SMTP และส่งลิงก์รวมทุก PO ของ Supplier รายนั้น | Pass |
| **TC-SUP-06** | คัดลอกลิงก์รวมทุก PO (Copy Supplier Portal Link) | 1. กดปุ่ม `🔗 คัดลอกลิงก์` ที่ Supplier | คัดลอกลิงก์ Company-wide Token (`po_number = None`) สำหรับเปิดดูทุก PO ของบริษัทนั้น | Pass |
| **TC-SUP-07** | ส่งออกไฟล์ CSV ผู้จำหน่าย (Export CSV) | 1. กดปุ่ม `Export CSV` | ดาวน์โหลดไฟล์ชื่อ **`IRM_Supplier_Master_Export_YYYYMMDD_HHMMSS.csv`** | Pass |
| **TC-SUP-08** | นำเข้าไฟล์ CSV ผู้จำหน่าย (Import CSV) | 1. นำเข้าไฟล์ CSV ที่มีคอลัมน์ Email และ Allow Over Delivery | ระบบอัปเดตข้อมูลทุกบริษัท พร้อมแจ้งเตือนผลการนำเข้า | Pass |

---

## 🌐 5. ชุดทดสอบ Supplier Portal (TC-PORTAL)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-PORTAL-01** | เปิดลิงก์ Portal ที่ถูกต้อง (Valid Token) | 1. เปิด URL `/supplier/portal/tok_xxxx`<br>2. ตรวจสอบข้อมูลหน้าจอ | แสดงชื่อบริษัท Supplier, แถบเตือนเวลาหมดอายุ, และรายการ PO ทั้งหมดที่เปิดค้างอยู่ | Pass |
| **TC-PORTAL-02** | เปิดลิงก์ที่หมดอายุหรือผิดพลาด (Invalid/Expired) | 1. เปิด URL Token ที่หมดอายุ หรือแก้ไขตัวอักษร Token | แสดงหน้าจอแจ้งเตือน `ลิงก์ไม่ถูกต้อง หรือหมดอายุการใช้งานแล้ว` พร้อมไอคอนสีแดง | Pass |
| **TC-PORTAL-03** | บันทึกร่างข้อมูลชั่วคราว (Save Draft) | 1. กรอกวันที่และจำนวนส่งบางรายการ<br>2. กดปุ่ม `[ 💾 บันทึกชั่วคราว ]` | บันทึกร่างข้อมูลสำเร็จ แสดงแถบเขียวแจ้งเตือน และ **หน้าจอยังคงเปิดให้แก้ไขต่อได้** | Pass |
| **TC-PORTAL-04** | แตกงวดส่งใน Portal (Split Sub-Items) | 1. กดปุ่ม `↳ แตกส่งหลายงวด`<br>2. ระบุ 2 งวดส่ง พร้อมเลือกวันที่ใน Datepicker | ระบบเพิ่มแถวงวดส่งย่อย และคำนวณยอดรวมให้อัตโนมัติ | Pass |
| **TC-PORTAL-05** | กรอกยอดส่งเกินกรณีได้รับสิทธิ์ | 1. Supplier ที่มีสิทธิ์ Over-Delivery กรอกยอดเกิน PO<br>2. กดปุ่ม บันทึกชั่วคราว หรือ ยืนยัน | ระบบไม่บล็อก และอนุญาตให้บันทึกข้อมูลยอดเกินได้สำเร็จ | Pass |
| **TC-PORTAL-06** | กรอกยอดส่งเกินกรณีไม่ได้รับสิทธิ์ | 1. Supplier ที่ไม่มีสิทธิ์ Over-Delivery กรอกยอดเกิน PO | ระบบแสดงข้อความเตือนสีแดง และไม่อนุญาตให้กดยืนยันส่งข้อมูล | Pass |
| **TC-PORTAL-07** | กดยืนยันส่งข้อมูลจริง (Final Submit) | 1. กดปุ่ม `[ 🚀 ยืนยันส่งข้อมูลให้ฝ่ายจัดซื้อ ]` | 1. บันทึกข้อมูลสำเร็จ<br>2. **ล็อกหน้าจอเป็น Read-only ทันที**<br>3. **ซ่อนปุ่มบันทึก, ปุ่มแตกงวด, ปุ่มลบทั้งหมด**<br>4. สถานะเปลี่ยนเป็น `🟢 ตอบกลับแล้ว`<br>5. ยิงแจ้งเตือนเข้า Telegram Group | Pass |
| **TC-PORTAL-08** | ป้องกันการส่งซ้ำหลังยืนยันแล้ว | 1. Refresh หน้า Portal หลังจากกดยืนยันแล้ว | หน้าจอแสดงข้อมูลที่ส่งไปแล้วในโหมด Read-only ไม่มีปุ่มให้กดแก้ไขหรือส่งซ้ำได้อีก | Pass |

---

## 📜 6. ชุดทดสอบประวัติและรายการปิดยอด (TC-HIST)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-HIST-01** | การย้าย PO ที่รับครบเข้าสู่ History | 1. SAP มีการรับของครบยอดใน PO<br>2. ระบบทำการ Sync ข้อมูล | รายการถูก Soft-close ย้ายออกจากหน้า Operation มาแสดงในหน้า History | Pass |
| **TC-HIST-02** | เปรียบเทียบแผน vs รับจริง (Plan vs Actual) | 1. เปิดหน้า `/history`<br>2. ตรวจสอบคอลัมน์ตัวเลข | แสดงเปรียบเทียบ: `ยอดสั่งซื้อ (PO)` \| `แผนที่นัดไว้ (Est)` \| `รับเข้าจริง (SAP)` \| `ผลต่าง (Variance)` | Pass |
| **TC-HIST-03** | เปิดดูประวัติ Audit Trail เรียงล่าสุดบนสุด | 1. กดไอคอนนาฬิกา `🕘` ที่รายการใดๆ | หน้าต่างประวัติเปิดขึ้นมา โดย **เหตุการณ์ล่าสุดอยู่บนสุดเสมอ** พร้อมป้ายแท็กไฮไลต์ **`ล่าสุด`** | Pass |
| **TC-HIST-04** | บันทึกประวัติการกระทำครบถ้วน (Full Event Audit) | 1. ตรวจสอบรายการในประวัติ | บันทึกทุกเหตุการณ์: *Sync จาก SAP*, *User ปรับปรุง*, *Supplier ตอบกลับ (Draft/Final)*, *จัดซื้อ Accept*, *จัดซื้อ Unlock* | Pass |
| **TC-HIST-05** | การลบประวัติตาม Retention Policy 7 วัน | 1. รายการที่ Close เกินกว่า 7 วัน<br>2. ระบบรัน Cron 04:00 น. | ระบบทำการ Hard Delete รายการที่หมดอายุออกจากฐานข้อมูลอัตโนมัติ | Pass |

---

## ⏰ 7. ชุดทดสอบระบบตั้งเวลาอัตโนมัติ (TC-SCHED)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-SCHED-01** | รอบ Sync SAP ประจำวัน (04:00 น.) | 1. ถึงเวลา 04:00 น. ตามเวลา Server | ดึงข้อมูล Open PO จาก SAP B1 นำเข้า IRM และส่งสรุปผลเข้า Telegram Group | Pass |
| **TC-SCHED-02** | รอบส่งอีเมลจันทร์ (08:00 น.) | 1. ถึงเวลาวันจันทร์ 08:00 น. | กระจายอีเมลหา Supplier ทุกรายที่มี Open PO กำหนดหมดอายุวันพุธ 23:59:59 น. | Pass |
| **TC-SCHED-03** | รอบส่งอีเมลพฤหัสบดี (08:00 น.) | 1. ถึงเวลาวันพฤหัสบดี 08:00 น. | กระจายอีเมลหา Supplier ทุกรายที่มี Open PO กำหนดหมดอายุวันอาทิตย์ 23:59:59 น. | Pass |
| **TC-SCHED-04** | Batch Rate Limiting ของ Email | 1. มี Supplier เกิน 20 ราย | ระบบแบ่งส่งทีละ 20 รายการ หน่วงเวลา 5 วินาทีต่อชุด ไม่ทำให้ Mail Server ติด Rate Limit | Pass |

---

## 📱 8. ชุดทดสอบการแจ้งเตือน Telegram (TC-TG)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-TG-01** | รูปแบบ Header มาตรฐาน | 1. ตรวจสอบข้อความ Telegram ทุกข้อความ | ขึ้นต้นด้วย `📦 IRM System · วันที่ เวลา น.` ตามด้วยเส้นคั่น `────` เสมอ | Pass |
| **TC-TG-02** | Rich Emojis ในทุกหัวข้อย่อย | 1. ตรวจสอบข้อความ Telegram ทุกข้อความ | ทุก Bullet Point มี Emoji ชัดเจน เช่น `• 🏢 ผู้จำหน่าย:`, `• 📑 เลขที่ PO:`, `• 📦 จำนวน:` | Pass |
| **TC-TG-03** | แจ้งเตือนเมื่อ Supplier ตอบกลับ | 1. Supplier กดยืนยันส่งข้อมูลผ่าน Portal | แจ้งเตือนเข้า Telegram Group ระบุชื่อ Supplier, เลข PO, จำนวนรายการ และสรุปงวดส่ง | Pass |
| **TC-TG-04** | แจ้งเตือนผลการ Sync SAP | 1. ทำการ Sync ข้อมูล SAP สำเร็จ/ล้มเหลว | ส่งรายงานสรุปจำนวน PO, รายการวัตถุดิบ, และยอดปิดเข้ากลุ่ม | Pass |
| **TC-TG-05** | แจ้งเตือนรายงานการกระจาย Email | 1. สั่งกระจาย Email หา Supplier | ส่งสรุปจำนวนบริษัทที่ส่งสำเร็จ และรายชื่อบริษัทที่ยังไม่มี Email ในระบบ | Pass |
| **TC-TG-06** | แจ้งเตือน Lead Time ใกล้ถึงกำหนด | 1. มีรายการที่ Est. Date ใกล้ถึงกำหนด | แจ้งเตือนฝ่ายจัดซื้อและคลังสินค้าให้เตรียมพื้นที่รับมอบ | Pass |
| **TC-TG-07** | แจ้งเตือนจัดซื้อ Unlock รายการ | 1. จัดซื้อกด Unlock และระบุเหตุผล | แจ้งเตือนว่าผู้ใช้ท่านใดเป็นผู้ปลดล็อครายการ พร้อมเหตุผล | Pass |

---

## ⚙️ 9. ชุดทดสอบการตั้งค่าระบบและการจัดการผู้ใช้ (TC-ADMIN)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-ADMIN-01** | ทดสอบการส่งข้อความ Telegram Group | 1. หน้า `/admin/settings` กดปุ่ม `ทดสอบส่งข้อความเข้ากลุ่ม` | ยิงข้อความทดสอบพร้อม Header และ Rich Emojis เข้ากลุ่ม Telegram ทันที | Pass |
| **TC-ADMIN-02** | ทดสอบการส่ง Email SMTP | 1. หน้า `/admin/settings` กดปุ่ม `ทดสอบการเชื่อมต่อ Email` | ส่งอีเมลทดสอบไปยัง Email ของ Admin สำเร็จ | Pass |
| **TC-ADMIN-03** | สลับโหมดการเชื่อมต่อ SAP (API vs SQL) | 1. เลือกโหมด `API (REST JSON)`<br>2. ใส่ URL และ Token<br>3. กดทดสอบการเชื่อมต่อ | ระบบสลับไปดึงข้อมูลผ่าน REST API Endpoint ได้อย่างถูกต้อง | Pass |
| **TC-ADMIN-04** | เพิ่มผู้ใช้งานใหม่และกำหนดกลุ่มสิทธิ์ | 1. หน้า `/admin/users` เพิ่มผู้ใช้ใหม่<br>2. กำหนดกลุ่ม `Purchasing`<br>3. Login ด้วยผู้ใช้ใหม่ | ผู้ใช้งานใหม่สามารถเข้าสู่ระบบและมีสิทธิ์ตามกลุ่มที่กำหนด | Pass |
| **TC-ADMIN-05** | ตารางสิทธิ์ Auth Matrix | 1. หน้า `/admin/auth-matrix` ปรับสิทธิ์ Create/Read/Update/Delete | สิทธิ์ในแต่ละหน้าจอและปุ่มกดปรับเปลี่ยนตามตาราง Matrix ทันที | Pass |
| **TC-ADMIN-06** | บันทึกและ Export Transaction Logs | 1. หน้า `/admin/logs`<br>2. ตรวจสอบ Log และกดปุ่ม `Export CSV` | ดาวน์โหลดไฟล์ **`IRM_Transaction_Logs_YYYYMMDD_HHMMSS.csv`** ครบทุกคอลัมน์ | Pass |

---

## 🛡️ 10. ชุดทดสอบความปลอดภัยและการจัดการข้อผิดพลาด (TC-SEC)

| รหัส TC | ชื่อการทดสอบ | ขั้นตอนการทดสอบ (Test Steps) | ผลลัพธ์ที่คาดหวัง (Expected Results) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-SEC-01** | ป้องกัน SQL Injection | 1. พิมพ์ `' OR '1'='1` ในช่องค้นหา หรือ Parameter API | ระบบประมวลผลเป็น Text ธรรมดาแบบ Parameterized Query ไม่เกิดช่องโหว่ | Pass |
| **TC-SEC-02** | ป้องกัน XSS Attack | 1. กรอก `<script>alert(1)</script>` ในชื่อสินค้า หรือเหตุผล Unlock | Next.js และ React ทำการ Escape HTML อัตโนมัติ ไม่มีการรัน Script | Pass |
| **TC-SEC-03** | Cryptographic Token Unpredictability | 1. ตรวจสอบรูปแบบ Token ที่สร้าง | Token มีความยาว 40+ ตัวอักษร สุ่มด้วย `secrets.token_hex` ป้องกันการเดา URL | Pass |
| **TC-SEC-04** | การกู้คืนเมื่อ SAP Connection ล้มเหลว | 1. ดึงสาย LAN หรือปิด Service SAP ชั่วคราว<br>2. รันคำสั่ง Sync | ระบบไม่ Crash, บันทึก Log `FAILED` และแจ้งเตือน Alert เข้า Telegram ทันที | Pass |
| **TC-SEC-05** | การตัดช่องว่างและ Sanitization ข้อมูล | 1. กรอกข้อมูลที่มีช่องว่างหัว-ท้าย หรือ Newline ผิดปกติ | ระบบตัด Trim ช่องว่าง และจัดรูปแบบให้อย่างถูกต้องก่อนบันทึก | Pass |

---
*เอกสารนี้ครอบคลุม Test Cases ทั้งหมด 66 รายการ ครบถ้วนทุกฟังก์ชันการทำงานของระบบ IRM*
